To install deps

```bash
uv sync
```
To run as module

```bash
uv run python -m path_to_file
```

## Setup

1. Copy enviroment files

.env.example -> .env
.secrets.example -> .secrets

Fill out what you intend need, api-keys etc. 